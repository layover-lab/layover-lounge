-- ─────────────────────────────────────────────────────────
-- 참가자 정보 갱신 허용
--
-- 학생 피드백 — "나갔다 들어와서 새로 역할을 정해도 제일 처음 정했던
-- 역할과 이름으로 뜹니다"
--
-- 원인: participants 에 insert 정책만 있고 update 정책이 없어서,
--       이미 있는 행을 재사용만 하고 이름·역할·색을 갱신하지 못했습니다.
--
-- ⚠️ 로그인이 없어서 "본인 것만" 을 DB 수준에서 가릴 수가 없습니다 (절대 규칙 ⑤).
--    10월에 로그인이 붙으면 기획서 13장 정책으로 조입니다.
-- ─────────────────────────────────────────────────────────

create policy "dev update" on participants
  for update using (true) with check (true);
