-- ─────────────────────────────────────────────────────────
-- 공식 방 (기획서 3.6 상황 목록)
--
-- seed.sql 이 아니라 마이그레이션에 두는 이유 —
--   supabase db push 는 seed.sql 을 돌리지 않습니다.
--   프로젝트를 갈아탈 때마다 손으로 넣어야 하는 게 사고 지점이 됩니다.
--   공식 방은 "샘플 데이터"가 아니라 서비스가 돌아가는 데 필요한 구조입니다.
--   (사람이 만든 방·테스트 유저 같은 진짜 시드는 seed.sql 에 둡니다)
-- ─────────────────────────────────────────────────────────

insert into rooms (slug, title, situation, world, is_official) values
  ('japan-trip', '일본 여행 가는 날', '멤버들이 일본 여행을 왔다', 'idol', true)
on conflict (slug) do nothing;
